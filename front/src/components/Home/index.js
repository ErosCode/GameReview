import React from 'react';
import PropTypes from 'prop-types';
import Header from '../Header';
import './styles.scss';

const Home = () => (
  <div className="home">
  </div>
);

export default Home;
